# react-lightweight-boiler
A lightweight boilerplate featuring React, Redux, Webpack, Sass.

## Installation
```bash
npm install
```

## Running Dev Server
```bash
npm start
```

## Remaining tasks
Create a production version (not use a dev-server).
Fix webkit to allow sass @import
